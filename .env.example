# Copy to .env (gitignored). dotenv loads it in development and test.
OPENROUTER_API_KEY=
# Any OpenRouter model id with tool calling; the default is anthropic/claude-opus-5.
# POETRY_IN_MOTION_MODEL=anthropic/claude-sonnet-5
