<div id="chat-status" class="px-4<%= " py-1" if text.present? %>">
  <% if text.present? %>
    <%= poetry_marker(announce: :status) do |marker| %>
      <% marker.with_icon { poetry_spinner } %>
      <%= text %>
    <% end %>
  <% end %>
</div>
