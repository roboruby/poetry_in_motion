<%# A transient failure line; nothing persists for it. %>
<%= poetry_message_scroller_item(id: "error-#{SecureRandom.hex(4)}") do %>
  <div class="px-4 py-1.5">
    <%= poetry_bubble(variant: :destructive) { "The analyst hit an error: #{text}" } %>
  </div>
<% end %>
